# Write a program that uses input to prompt a user for their name and then welcomes them

# Prompt the user to enter data. The inputted variable is stored as a STRING    
name = input("Please enter your name: ")

# Print the welcome message. The coma in print function adds a space
print("Hello", name)